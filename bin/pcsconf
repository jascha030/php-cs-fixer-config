#!/usr/bin/env zsh

COMPOSERHOMEDIR=$(composer -n config --global home)
PSCONFIG=$COMPOSERHOMEDIR/vendor/jascha030/php-cs-config

if [[ -d $PSCONFIG ]]
then
  cp "$PSCONFIG/.php-cs-fixer.dist.php" "$PWD/.php-cs-fixer.dist.php"
fi

exit 0;
